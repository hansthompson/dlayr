# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
sudo: false
cache: packages
addons:
  apt:
    packages:
      - libxml2-dev
      - libssl-dev 
      - libcurl4-gnutls-dev 
      - libxml2-dev 
      - libxslt-dev
